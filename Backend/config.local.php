<?php

$db_host = "(i removed it)(it should contain the host name)";
$db_name = "(i removed it)(it should contain the database name)";
$db_user = "(i removed it)(it should contain the username)";
$db_pass = "(i removed it)(it should contain the database password)";
$brevo_api_key = "(i removed it)(it should contain the brevo api key)";
$brevo_sender_email = "(i removed it)(it should contain the brevo sending email)(the email from which email will be sent) ";
$brevo_sender_name = "(i removed it)(it should contain the sender name)";